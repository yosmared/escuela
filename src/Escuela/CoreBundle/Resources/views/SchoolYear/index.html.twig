{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SchoolYear list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Year</th>
                <th>Current</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('schoolyear_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.year %}{{ entity.year|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.current }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('schoolyear_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('schoolyear_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('schoolyear_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
