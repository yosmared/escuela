{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SchoolYear</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ entity.year|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Current</th>
                <td>{{ entity.current }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('schoolyear') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('schoolyear_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
