{% extends 'EscuelaCommonsBundle:Base:edit-base.html.twig' %}

{# Page title and heading	 #}
{% set page_title="Editar Padre ó Madre" %}

{% block form_fields %}
    {% set form_object= edit_form %}
	{% include 'EscuelaCoreBundle:Parents:new-edit-fields.html.twig' %}    
{% endblock %}

{% block action_buttons %}
	{# Si el usuario autenticado tiene permisos para ver... #}
	{% if (is_granted('VIEW',service)) %}
		{# ...muestra la lista de usuarios #}
		{% set cancel_url = path('parents_list', {'studentid':studentid}) %}
	{# sino... #}
	{% else %}
		{# ...muestra los datos del usuario #}
    	{% set cancel_url = path('parents_show', {'id': entity.id,'studentid':studentid}) %}
    	{# tambien se puede usar: path('user_show', {'id': app.user.id}) #}
	{% endif %}
	{{ parent() }}
{% endblock %}	
